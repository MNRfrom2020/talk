// This is a placeholder service worker file.
// `next-pwa` will generate the actual service worker file.
// You can add custom service worker logic here, and it will be included in the generated file.

self.addEventListener('fetch', (event) => {
  // You can add custom fetch handling logic here if needed.
  // For example, to handle specific routes or API calls differently.
});
